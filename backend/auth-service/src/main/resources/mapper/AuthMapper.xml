<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usermanagement.auth.mapper.AuthMapper">
    <select id="findUserByUsername" resultType="map">
        select id, username, password_hash, display_name, email, phone, avatar, status, tenant_id
        from sys_user
        where username = #{username}
    </select>

    <select id="findPublicUserById" resultType="map">
        select id, username, display_name, email, phone, avatar, status, tenant_id
        from sys_user
        where id = #{id}
    </select>

    <select id="findRoleCodesByUserId" resultType="string">
        select r.code
        from sys_role r
        join sys_user_role ur on ur.role_id = r.id
        where ur.user_id = #{userId}
    </select>

    <select id="findPermissionCodesByUserId" resultType="string">
        select distinct p.code
        from sys_permission p
        join sys_role_permission rp on rp.permission_id = p.id
        join sys_user_role ur on ur.role_id = rp.role_id
        where ur.user_id = #{userId}
    </select>

    <update id="updateLastLoginAt">
        update sys_user
        set last_login_at = now()
        where id = #{id}
    </update>

    <update id="updatePasswordByUsername">
        update sys_user
        set password_hash = #{passwordHash}
        where username = #{username}
    </update>

    <insert id="insertLoginLog">
        insert into sys_login_log(username, ip_address, user_agent, status, message)
        values (#{username}, #{ipAddress}, #{userAgent}, #{status}, #{message})
    </insert>
</mapper>
