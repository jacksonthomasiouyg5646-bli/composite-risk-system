<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usermanagement.system.mapper.SystemCrudMapper">
    <select id="listLoginLogs" resultType="map">select * from sys_login_log <if test="keyword != null and keyword != ''">where username like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countLoginLogs" resultType="long">select count(*) from sys_login_log <if test="keyword != null and keyword != ''">where username like concat('%', #{keyword}, '%')</if></select>
    <select id="getLoginLog" resultType="map">select * from sys_login_log where id = #{id}</select>
    <insert id="insertLoginLog" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into sys_login_log <trim prefix="(" suffix=")" suffixOverrides=","><if test="username != null">username,</if><if test="ip_address != null">ip_address,</if><if test="user_agent != null">user_agent,</if><if test="status != null">status,</if><if test="message != null">message,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="username != null">#{username},</if><if test="ip_address != null">#{ip_address},</if><if test="user_agent != null">#{user_agent},</if><if test="status != null">#{status},</if><if test="message != null">#{message},</if></trim></insert>
    <update id="updateLoginLog">update sys_login_log <set><if test="body.username != null">username=#{body.username},</if><if test="body.ip_address != null">ip_address=#{body.ip_address},</if><if test="body.user_agent != null">user_agent=#{body.user_agent},</if><if test="body.status != null">status=#{body.status},</if><if test="body.message != null">message=#{body.message},</if></set> where id=#{id}</update>
    <delete id="deleteLoginLog">delete from sys_login_log where id = #{id}</delete>

    <select id="listOperationLogs" resultType="map">select * from sys_operation_log <if test="keyword != null and keyword != ''">where action like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countOperationLogs" resultType="long">select count(*) from sys_operation_log <if test="keyword != null and keyword != ''">where action like concat('%', #{keyword}, '%')</if></select>
    <select id="getOperationLog" resultType="map">select * from sys_operation_log where id = #{id}</select>
    <insert id="insertOperationLog" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into sys_operation_log <trim prefix="(" suffix=")" suffixOverrides=","><if test="username != null">username,</if><if test="module != null">module,</if><if test="action != null">action,</if><if test="method != null">method,</if><if test="request_uri != null">request_uri,</if><if test="status != null">status,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="username != null">#{username},</if><if test="module != null">#{module},</if><if test="action != null">#{action},</if><if test="method != null">#{method},</if><if test="request_uri != null">#{request_uri},</if><if test="status != null">#{status},</if></trim></insert>
    <update id="updateOperationLog">update sys_operation_log <set><if test="body.username != null">username=#{body.username},</if><if test="body.module != null">module=#{body.module},</if><if test="body.action != null">action=#{body.action},</if><if test="body.method != null">method=#{body.method},</if><if test="body.request_uri != null">request_uri=#{body.request_uri},</if><if test="body.status != null">status=#{body.status},</if></set> where id=#{id}</update>
    <delete id="deleteOperationLog">delete from sys_operation_log where id = #{id}</delete>

    <select id="listErrorLogs" resultType="map">select * from sys_error_log <if test="keyword != null and keyword != ''">where message like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countErrorLogs" resultType="long">select count(*) from sys_error_log <if test="keyword != null and keyword != ''">where message like concat('%', #{keyword}, '%')</if></select>
    <select id="getErrorLog" resultType="map">select * from sys_error_log where id = #{id}</select>
    <insert id="insertErrorLog" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into sys_error_log <trim prefix="(" suffix=")" suffixOverrides=","><if test="service_name != null">service_name,</if><if test="trace_id != null">trace_id,</if><if test="message != null">message,</if><if test="stack_trace != null">stack_trace,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="service_name != null">#{service_name},</if><if test="trace_id != null">#{trace_id},</if><if test="message != null">#{message},</if><if test="stack_trace != null">#{stack_trace},</if></trim></insert>
    <update id="updateErrorLog">update sys_error_log <set><if test="body.service_name != null">service_name=#{body.service_name},</if><if test="body.trace_id != null">trace_id=#{body.trace_id},</if><if test="body.message != null">message=#{body.message},</if><if test="body.stack_trace != null">stack_trace=#{body.stack_trace},</if></set> where id=#{id}</update>
    <delete id="deleteErrorLog">delete from sys_error_log where id = #{id}</delete>

    <select id="listNotifications" resultType="map">select * from sys_notification <if test="keyword != null and keyword != ''">where title like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countNotifications" resultType="long">select count(*) from sys_notification <if test="keyword != null and keyword != ''">where title like concat('%', #{keyword}, '%')</if></select>
    <select id="getNotification" resultType="map">select * from sys_notification where id = #{id}</select>
    <insert id="insertNotification" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into sys_notification <trim prefix="(" suffix=")" suffixOverrides=","><if test="title != null">title,</if><if test="content != null">content,</if><if test="channel != null">channel,</if><if test="target_type != null">target_type,</if><if test="status != null">status,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="title != null">#{title},</if><if test="content != null">#{content},</if><if test="channel != null">#{channel},</if><if test="target_type != null">#{target_type},</if><if test="status != null">#{status},</if></trim></insert>
    <update id="updateNotification">update sys_notification <set><if test="body.title != null">title=#{body.title},</if><if test="body.content != null">content=#{body.content},</if><if test="body.channel != null">channel=#{body.channel},</if><if test="body.target_type != null">target_type=#{body.target_type},</if><if test="body.status != null">status=#{body.status},</if></set> where id=#{id}</update>
    <delete id="deleteNotification">delete from sys_notification where id = #{id}</delete>

    <select id="listConfigs" resultType="map">select * from sys_config <if test="keyword != null and keyword != ''">where config_key like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countConfigs" resultType="long">select count(*) from sys_config <if test="keyword != null and keyword != ''">where config_key like concat('%', #{keyword}, '%')</if></select>
    <select id="getConfig" resultType="map">select * from sys_config where id = #{id}</select>
    <insert id="insertConfig" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into sys_config <trim prefix="(" suffix=")" suffixOverrides=","><if test="config_key != null">config_key,</if><if test="config_value != null">config_value,</if><if test="description != null">description,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="config_key != null">#{config_key},</if><if test="config_value != null">#{config_value},</if><if test="description != null">#{description},</if></trim></insert>
    <update id="updateConfig">update sys_config <set><if test="body.config_key != null">config_key=#{body.config_key},</if><if test="body.config_value != null">config_value=#{body.config_value},</if><if test="body.description != null">description=#{body.description},</if></set> where id=#{id}</update>
    <delete id="deleteConfig">delete from sys_config where id = #{id}</delete>

    <select id="listSecurityPolicies" resultType="map">select * from sys_security_policy <if test="keyword != null and keyword != ''">where name like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countSecurityPolicies" resultType="long">select count(*) from sys_security_policy <if test="keyword != null and keyword != ''">where name like concat('%', #{keyword}, '%')</if></select>
    <select id="getSecurityPolicy" resultType="map">select * from sys_security_policy where id = #{id}</select>
    <insert id="insertSecurityPolicy" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into sys_security_policy <trim prefix="(" suffix=")" suffixOverrides=","><if test="name != null">name,</if><if test="policy_key != null">policy_key,</if><if test="policy_value != null">policy_value,</if><if test="enabled != null">enabled,</if><if test="description != null">description,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="name != null">#{name},</if><if test="policy_key != null">#{policy_key},</if><if test="policy_value != null">#{policy_value},</if><if test="enabled != null">#{enabled},</if><if test="description != null">#{description},</if></trim></insert>
    <update id="updateSecurityPolicy">update sys_security_policy <set><if test="body.name != null">name=#{body.name},</if><if test="body.policy_key != null">policy_key=#{body.policy_key},</if><if test="body.policy_value != null">policy_value=#{body.policy_value},</if><if test="body.enabled != null">enabled=#{body.enabled},</if><if test="body.description != null">description=#{body.description},</if></set> where id=#{id}</update>
    <delete id="deleteSecurityPolicy">delete from sys_security_policy where id = #{id}</delete>

    <select id="listTenants" resultType="map">select * from sys_tenant <if test="keyword != null and keyword != ''">where name like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countTenants" resultType="long">select count(*) from sys_tenant <if test="keyword != null and keyword != ''">where name like concat('%', #{keyword}, '%')</if></select>
    <select id="getTenant" resultType="map">select * from sys_tenant where id = #{id}</select>
    <insert id="insertTenant" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into sys_tenant <trim prefix="(" suffix=")" suffixOverrides=","><if test="name != null">name,</if><if test="code != null">code,</if><if test="contact_name != null">contact_name,</if><if test="contact_phone != null">contact_phone,</if><if test="status != null">status,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="name != null">#{name},</if><if test="code != null">#{code},</if><if test="contact_name != null">#{contact_name},</if><if test="contact_phone != null">#{contact_phone},</if><if test="status != null">#{status},</if></trim></insert>
    <update id="updateTenant">update sys_tenant <set><if test="body.name != null">name=#{body.name},</if><if test="body.code != null">code=#{body.code},</if><if test="body.contact_name != null">contact_name=#{body.contact_name},</if><if test="body.contact_phone != null">contact_phone=#{body.contact_phone},</if><if test="body.status != null">status=#{body.status},</if></set> where id=#{id}</update>
    <delete id="deleteTenant">delete from sys_tenant where id = #{id}</delete>

    <select id="listRiskRegisters" resultType="map">
        select * from risk_register <if test="keyword != null and keyword != ''">where risk_name like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}
    </select>
    <select id="countRiskRegisters" resultType="long">select count(*) from risk_register <if test="keyword != null and keyword != ''">where risk_name like concat('%', #{keyword}, '%')</if></select>
    <select id="getRiskRegister" resultType="map">select * from risk_register where id = #{id}</select>
    <insert id="insertRiskRegister" parameterType="map" useGeneratedKeys="true" keyProperty="id">
        insert into risk_register
        <trim prefix="(" suffix=")" suffixOverrides=","><if test="risk_code != null">risk_code,</if><if test="risk_name != null">risk_name,</if><if test="category != null">category,</if><if test="level != null">`level`,</if><if test="owner_department != null">owner_department,</if><if test="responsible_person != null">responsible_person,</if><if test="status != null">status,</if><if test="identified_at != null">identified_at,</if><if test="due_date != null">due_date,</if><if test="description != null">description,</if></trim>
        <trim prefix="values (" suffix=")" suffixOverrides=","><if test="risk_code != null">#{risk_code},</if><if test="risk_name != null">#{risk_name},</if><if test="category != null">#{category},</if><if test="level != null">#{level},</if><if test="owner_department != null">#{owner_department},</if><if test="responsible_person != null">#{responsible_person},</if><if test="status != null">#{status},</if><if test="identified_at != null">#{identified_at},</if><if test="due_date != null">#{due_date},</if><if test="description != null">#{description},</if></trim>
    </insert>
    <update id="updateRiskRegister">update risk_register <set><if test="body.risk_code != null">risk_code=#{body.risk_code},</if><if test="body.risk_name != null">risk_name=#{body.risk_name},</if><if test="body.category != null">category=#{body.category},</if><if test="body.level != null">`level`=#{body.level},</if><if test="body.owner_department != null">owner_department=#{body.owner_department},</if><if test="body.responsible_person != null">responsible_person=#{body.responsible_person},</if><if test="body.status != null">status=#{body.status},</if><if test="body.identified_at != null">identified_at=#{body.identified_at},</if><if test="body.due_date != null">due_date=#{body.due_date},</if><if test="body.description != null">description=#{body.description},</if></set> where id=#{id}</update>
    <delete id="deleteRiskRegister">delete from risk_register where id = #{id}</delete>

    <select id="listRiskAssessments" resultType="map">select * from risk_assessment <if test="keyword != null and keyword != ''">where risk_name like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countRiskAssessments" resultType="long">select count(*) from risk_assessment <if test="keyword != null and keyword != ''">where risk_name like concat('%', #{keyword}, '%')</if></select>
    <select id="getRiskAssessment" resultType="map">select * from risk_assessment where id = #{id}</select>
    <insert id="insertRiskAssessment" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into risk_assessment <trim prefix="(" suffix=")" suffixOverrides=","><if test="risk_code != null">risk_code,</if><if test="risk_name != null">risk_name,</if><if test="likelihood != null">likelihood,</if><if test="impact != null">impact,</if><if test="inherent_level != null">inherent_level,</if><if test="residual_level != null">residual_level,</if><if test="assessor != null">assessor,</if><if test="assessed_at != null">assessed_at,</if><if test="conclusion != null">conclusion,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="risk_code != null">#{risk_code},</if><if test="risk_name != null">#{risk_name},</if><if test="likelihood != null">#{likelihood},</if><if test="impact != null">#{impact},</if><if test="inherent_level != null">#{inherent_level},</if><if test="residual_level != null">#{residual_level},</if><if test="assessor != null">#{assessor},</if><if test="assessed_at != null">#{assessed_at},</if><if test="conclusion != null">#{conclusion},</if></trim></insert>
    <update id="updateRiskAssessment">update risk_assessment <set><if test="body.risk_code != null">risk_code=#{body.risk_code},</if><if test="body.risk_name != null">risk_name=#{body.risk_name},</if><if test="body.likelihood != null">likelihood=#{body.likelihood},</if><if test="body.impact != null">impact=#{body.impact},</if><if test="body.inherent_level != null">inherent_level=#{body.inherent_level},</if><if test="body.residual_level != null">residual_level=#{body.residual_level},</if><if test="body.assessor != null">assessor=#{body.assessor},</if><if test="body.assessed_at != null">assessed_at=#{body.assessed_at},</if><if test="body.conclusion != null">conclusion=#{body.conclusion},</if></set> where id=#{id}</update>
    <delete id="deleteRiskAssessment">delete from risk_assessment where id = #{id}</delete>

    <select id="listControlMeasures" resultType="map">select * from risk_control_measure <if test="keyword != null and keyword != ''">where control_name like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countControlMeasures" resultType="long">select count(*) from risk_control_measure <if test="keyword != null and keyword != ''">where control_name like concat('%', #{keyword}, '%')</if></select>
    <select id="getControlMeasure" resultType="map">select * from risk_control_measure where id = #{id}</select>
    <insert id="insertControlMeasure" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into risk_control_measure <trim prefix="(" suffix=")" suffixOverrides=","><if test="control_code != null">control_code,</if><if test="risk_code != null">risk_code,</if><if test="control_name != null">control_name,</if><if test="control_type != null">control_type,</if><if test="frequency != null">frequency,</if><if test="owner != null">owner,</if><if test="effectiveness != null">effectiveness,</if><if test="status != null">status,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="control_code != null">#{control_code},</if><if test="risk_code != null">#{risk_code},</if><if test="control_name != null">#{control_name},</if><if test="control_type != null">#{control_type},</if><if test="frequency != null">#{frequency},</if><if test="owner != null">#{owner},</if><if test="effectiveness != null">#{effectiveness},</if><if test="status != null">#{status},</if></trim></insert>
    <update id="updateControlMeasure">update risk_control_measure <set><if test="body.control_code != null">control_code=#{body.control_code},</if><if test="body.risk_code != null">risk_code=#{body.risk_code},</if><if test="body.control_name != null">control_name=#{body.control_name},</if><if test="body.control_type != null">control_type=#{body.control_type},</if><if test="body.frequency != null">frequency=#{body.frequency},</if><if test="body.owner != null">owner=#{body.owner},</if><if test="body.effectiveness != null">effectiveness=#{body.effectiveness},</if><if test="body.status != null">status=#{body.status},</if></set> where id=#{id}</update>
    <delete id="deleteControlMeasure">delete from risk_control_measure where id = #{id}</delete>

    <select id="listTreatmentPlans" resultType="map">select * from risk_treatment_plan <if test="keyword != null and keyword != ''">where action like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countTreatmentPlans" resultType="long">select count(*) from risk_treatment_plan <if test="keyword != null and keyword != ''">where action like concat('%', #{keyword}, '%')</if></select>
    <select id="getTreatmentPlan" resultType="map">select * from risk_treatment_plan where id = #{id}</select>
    <insert id="insertTreatmentPlan" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into risk_treatment_plan <trim prefix="(" suffix=")" suffixOverrides=","><if test="plan_code != null">plan_code,</if><if test="risk_code != null">risk_code,</if><if test="action != null">action,</if><if test="owner != null">owner,</if><if test="due_date != null">due_date,</if><if test="progress != null">progress,</if><if test="status != null">status,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="plan_code != null">#{plan_code},</if><if test="risk_code != null">#{risk_code},</if><if test="action != null">#{action},</if><if test="owner != null">#{owner},</if><if test="due_date != null">#{due_date},</if><if test="progress != null">#{progress},</if><if test="status != null">#{status},</if></trim></insert>
    <update id="updateTreatmentPlan">update risk_treatment_plan <set><if test="body.plan_code != null">plan_code=#{body.plan_code},</if><if test="body.risk_code != null">risk_code=#{body.risk_code},</if><if test="body.action != null">action=#{body.action},</if><if test="body.owner != null">owner=#{body.owner},</if><if test="body.due_date != null">due_date=#{body.due_date},</if><if test="body.progress != null">progress=#{body.progress},</if><if test="body.status != null">status=#{body.status},</if></set> where id=#{id}</update>
    <delete id="deleteTreatmentPlan">delete from risk_treatment_plan where id = #{id}</delete>

    <select id="listRiskEvents" resultType="map">select * from risk_event <if test="keyword != null and keyword != ''">where title like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countRiskEvents" resultType="long">select count(*) from risk_event <if test="keyword != null and keyword != ''">where title like concat('%', #{keyword}, '%')</if></select>
    <select id="getRiskEvent" resultType="map">select * from risk_event where id = #{id}</select>
    <insert id="insertRiskEvent" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into risk_event <trim prefix="(" suffix=")" suffixOverrides=","><if test="event_code != null">event_code,</if><if test="title != null">title,</if><if test="risk_code != null">risk_code,</if><if test="severity != null">severity,</if><if test="occurred_at != null">occurred_at,</if><if test="loss_amount != null">loss_amount,</if><if test="owner != null">owner,</if><if test="status != null">status,</if><if test="summary != null">summary,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="event_code != null">#{event_code},</if><if test="title != null">#{title},</if><if test="risk_code != null">#{risk_code},</if><if test="severity != null">#{severity},</if><if test="occurred_at != null">#{occurred_at},</if><if test="loss_amount != null">#{loss_amount},</if><if test="owner != null">#{owner},</if><if test="status != null">#{status},</if><if test="summary != null">#{summary},</if></trim></insert>
    <update id="updateRiskEvent">update risk_event <set><if test="body.event_code != null">event_code=#{body.event_code},</if><if test="body.title != null">title=#{body.title},</if><if test="body.risk_code != null">risk_code=#{body.risk_code},</if><if test="body.severity != null">severity=#{body.severity},</if><if test="body.occurred_at != null">occurred_at=#{body.occurred_at},</if><if test="body.loss_amount != null">loss_amount=#{body.loss_amount},</if><if test="body.owner != null">owner=#{body.owner},</if><if test="body.status != null">status=#{body.status},</if><if test="body.summary != null">summary=#{body.summary},</if></set> where id=#{id}</update>
    <delete id="deleteRiskEvent">delete from risk_event where id = #{id}</delete>

    <select id="listRiskIndicators" resultType="map">select id, indicator_code, name, threshold_value as threshold, current_value, trend, owner, status, created_at, updated_at from risk_indicator <if test="keyword != null and keyword != ''">where name like concat('%', #{keyword}, '%')</if> order by id desc limit #{limit} offset #{offset}</select>
    <select id="countRiskIndicators" resultType="long">select count(*) from risk_indicator <if test="keyword != null and keyword != ''">where name like concat('%', #{keyword}, '%')</if></select>
    <select id="getRiskIndicator" resultType="map">select id, indicator_code, name, threshold_value as threshold, current_value, trend, owner, status, created_at, updated_at from risk_indicator where id = #{id}</select>
    <insert id="insertRiskIndicator" parameterType="map" useGeneratedKeys="true" keyProperty="id">insert into risk_indicator <trim prefix="(" suffix=")" suffixOverrides=","><if test="indicator_code != null">indicator_code,</if><if test="name != null">name,</if><if test="threshold != null">threshold_value,</if><if test="current_value != null">current_value,</if><if test="trend != null">trend,</if><if test="owner != null">owner,</if><if test="status != null">status,</if></trim><trim prefix="values (" suffix=")" suffixOverrides=","><if test="indicator_code != null">#{indicator_code},</if><if test="name != null">#{name},</if><if test="threshold != null">#{threshold},</if><if test="current_value != null">#{current_value},</if><if test="trend != null">#{trend},</if><if test="owner != null">#{owner},</if><if test="status != null">#{status},</if></trim></insert>
    <update id="updateRiskIndicator">update risk_indicator <set><if test="body.indicator_code != null">indicator_code=#{body.indicator_code},</if><if test="body.name != null">name=#{body.name},</if><if test="body.threshold != null">threshold_value=#{body.threshold},</if><if test="body.current_value != null">current_value=#{body.current_value},</if><if test="body.trend != null">trend=#{body.trend},</if><if test="body.owner != null">owner=#{body.owner},</if><if test="body.status != null">status=#{body.status},</if></set> where id=#{id}</update>
    <delete id="deleteRiskIndicator">delete from risk_indicator where id = #{id}</delete>

    <select id="listUsersForExport" resultType="map">
        select id, username, display_name, email, phone, status
        from sys_user
        order by id
    </select>
</mapper>
